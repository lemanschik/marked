# The repo of npm package marked

## Install
npm i https://github.com/lemanschik/marked#update_typescript

Installs marked-src marked-cli marked-man marked

marked === dist bundels of the marked-src/lib

marked-src === old typescript src and current lib as also rollup instructions to create packages/marked/dist
is the main repo package containing workspaces inside packages/